#include <bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int n;
	int cnt = 0;
	while (true) {
		cin >> n;
		if (n == 0) break;
		cnt++;
		cout << "Case " << cnt << ": Sorting... done!\n";
		for (int i = 0; i < n; ++i) {
			int x;
			cin >> x;
		}
	}

	return 0;
}