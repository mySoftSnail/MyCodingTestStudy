#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);

    string str;
    while (!cin.eof()) {
        getline(cin, str);
        cout << str << '\n';
    }
}